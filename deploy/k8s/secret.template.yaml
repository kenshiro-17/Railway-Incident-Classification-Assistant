apiVersion: v1
kind: Secret
metadata:
  name: ria-secrets
type: Opaque
stringData:
  RIA_GEMINI_API_KEY: "REPLACE_ME"
  RIA_SSO_JWKS_URL: "https://idp.example/.well-known/jwks.json"
  RIA_SSO_ISSUER: "https://idp.example/"
  RIA_SSO_AUDIENCE: "ria-assistant"
  RIA_INTERNAL_API_TOKEN: "REPLACE_ME"
  RIA_AUDIT_SIGNING_KEY: "REPLACE_ME"
  RIA_SECRETS_ENCRYPTION_KEY: "REPLACE_ME_FERNET_KEY"
